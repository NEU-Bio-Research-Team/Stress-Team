# ── Core numeric / data ──
numpy>=1.24
pandas>=2.0
pyarrow>=14.0

# ── Scientific computing ──
scipy>=1.11

# ── Plotting ──
matplotlib>=3.7

# ── HTTP / API ──
requests>=2.31

# ── Tardis BTC data (OPTIONAL – Binance Vision is the free default) ──
# Uncomment below ONLY if you have a Tardis API key:
# tardis-dev>=2.1,<3.0; python_version < "3.11"
# tardis-dev>=3.0; python_version >= "3.11"

# ── Orderbook reconstruction ──
sortedcontainers>=2.4

# ── Machine learning ──
scikit-learn>=1.3

# ── Phase 1 LLM elicitation ──
# Install PyTorch separately so the CUDA wheel matches your host, for example:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu122
huggingface_hub>=0.24
transformers>=4.44,<4.46
accelerate>=0.34,<1.0
peft>=0.12,<1.0
bitsandbytes>=0.43,<0.44; platform_system == "Linux"
vllm>=0.6,<0.7; platform_system == "Linux" and python_version >= "3.10"
